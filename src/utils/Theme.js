import {colors} from './colors'
import {fonts }from './fonts'

export const theme = {
    colors,
    fonts,

    media: {
        desktop: '@media (min-width: 1024px)',
        desktop: '@media (min-width: 1024px)',
        desktop: '@media (min-width: 1024px)',
    }
 
}

export default theme; 